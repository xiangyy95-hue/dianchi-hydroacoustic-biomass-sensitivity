# Dianchi hydroacoustic biomass sensitivity

Data and R code supporting the manuscript **"Sensitivity of hydroacoustic fish-biomass estimates to the target-strength and length-weight conversions in Dianchi Lake, China"**.

The analysis evaluates how alternative target-strength-to-length (TS-L) equations and length-weight relationships (LWRs) affect hydroacoustic estimates of fish biomass in Dianchi Lake, China. The repository contains the data needed by the analysis, the complete R script, and reproducible tables and figures generated by that script.

## Repository structure

```text
.
|-- code/
|   `-- analysis.R
|-- data/
|   |-- hydroacoustic_edsu_data.xlsx
|   |-- fish_catch_data.xlsx
|   |-- species_names.csv
|   `-- table3_dominance.csv
|-- figures_analysis/       # generated Fig. 2-Fig. 6 files
|-- tables_analysis/        # generated main and supplementary tables
|-- CITATION.cff
|-- checksums.sha256
|-- DATA_DICTIONARY.md
|-- RELEASE_CHECKLIST.md
|-- session-info.txt        # generated automatically after a successful run
|-- VERIFICATION.md
`-- README.md
```

All four files in `data/` are read by `code/analysis.R`. The numerical dominance metrics are recalculated from `fish_catch_data.xlsx`; `table3_dominance.csv` supplies only the species codes, taxonomic families, and feeding-guild classifications used in those calculations.

## Software requirements

The verified environment uses R 4.5.1 with these package versions:

- readxl 1.5.0
- ggplot2 4.0.3
- patchwork 1.3.2
- scales 1.4.0
- MCMCglmm 2.36
- ggrepel 0.9.6

Install missing packages explicitly before running the analysis:

```r
install.packages(c("readxl", "ggplot2", "patchwork", "scales", "MCMCglmm", "ggrepel"))
```

The analysis script does not install software or alter the user's package library automatically. It stops with a clear message if a required package is missing.

## How to reproduce the analysis

In a terminal, run the script by its path. The current working directory does not matter:

```bash
Rscript code/analysis.R
```

In RStudio, open this repository as the working folder and run:

```r
source("code/analysis.R", encoding = "UTF-8")
```

The script uses relative paths derived from its own location. Do not add a local `setwd()` call. Tables are written to `tables_analysis/`, figures to `figures_analysis/`, and the complete R/package environment to `session-info.txt`.

The two Excel workbooks retain the original Chinese site names, species names, and source headers as research records. Their filenames and all executable code are ASCII/English. Immediately after import, the script assigns English analytical variable names and uses `species_names.csv` to map Chinese common names to Latin names. This arrangement was tested from an ASCII-only project path on Windows.

## Expected verification values

A successful run should report or reproduce the following manuscript values:

- 573 valid EDSUs and 14 target-strength classes.
- 2,179 complete individually measured fish used for the length-weight analysis.
- 2,000 moving-block bootstrap replicates with block length 20.
- TS-L sensitivity of approximately 467-fold across the evaluated equations.
- Whole-lake biomass under the Ye et al. (2007) LWR: approximately 36,124 t (95% CI 18,897-61,709 t; CV 32.4%).
- Whole-lake biomass under the community-fitted LWR: approximately 37,805 t (95% CI 19,791-64,107 t; CV 32.3%).
- Community-versus-Ye difference: +4.7% (paired-bootstrap 95% CI 2.6%-7.0%; P = 0.019).
- Size-group correction rates ranging from -36.0% to +36.2%.

Small differences in MCMC summaries can occur if package versions or random-number generators differ. The main deterministic tables and the seeded bootstrap results should match the verified outputs.

## Data and privacy status

This folder is a local, upload-ready working copy. Keep the GitHub repository **private** until every author has approved public release and the data have passed the final confidentiality, ethics, ownership, and licensing checks. The manuscript file itself is deliberately not included.

A Zenodo draft has been prepared under the reserved version-specific DOI `10.5281/zenodo.22476648`. The DOI will be registered when the record is published. During peer review, the restricted draft should be shared only through the confidential reviewer link supplied in the journal submission system; that private link must not be committed to this repository.

## Contact and citation

Please use the metadata in `CITATION.cff`. Correspondence about the manuscript may be directed to Chao Guo (`guochao@ihb.ac.cn`) or Wei Li (`liwei@ihb.ac.cn`).
